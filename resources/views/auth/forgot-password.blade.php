<x-layouts.guest><x-card class="w-full max-w-md"><h1 class="font-[Sora] text-2xl font-bold text-[#0B1D2D]">Recuperar acesso</h1><p class="mt-2 text-sm text-gray-500">Informe seu e-mail para receber instruções.</p>@if(session('status'))<x-alert type="success">{{ session('status') }}</x-alert>@endif<form method="POST" action="{{ route('password.email') }}" class="mt-6 space-y-4">@csrf<x-input name="email" type="email" label="E-mail" required/><x-button class="w-full">Enviar instruções</x-button></form><a href="{{ route('login') }}" class="mt-4 block text-center text-sm text-teal-700">Voltar ao login</a></x-card></x-layouts.guest>
