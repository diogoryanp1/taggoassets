<x-layouts.app><x-breadcrumb>Catalogo / Marcas / Detalhes</x-breadcrumb><div class="mb-5 flex items-start justify-between"><div><h1 class="font-[Sora] text-2xl font-bold">{{ $brand->name }}</h1><p class="text-sm text-gray-500">{{ $brand->models_count }} modelos vinculados</p></div><x-badge>{{ $brand->is_active ? 'Ativa' : 'Inativa' }}</x-badge></div><x-card><div class="flex gap-3">@can('update', $brand)<x-button type="button" onclick="location.href='{{ route('catalog.brands.edit', $brand) }}'">Editar</x-button>@endcan @can('deactivate', $brand)<form method="POST" action="{{ $brand->is_active ? route('catalog.brands.deactivate', $brand) : route('catalog.brands.reactivate', $brand) }}">@csrf @method('PATCH')<x-button variant="secondary">{{ $brand->is_active ? 'Inativar' : 'Reativar' }}</x-button></form>@endcan</div></x-card></x-layouts.app>
