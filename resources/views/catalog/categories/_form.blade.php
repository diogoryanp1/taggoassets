<div class="grid gap-4"><x-input name="name" label="Nome" value="{{ old('name', $category->name ?? '') }}" required/><x-input name="code" label="Código" value="{{ old('code', $category->code ?? '') }}"/><x-input name="parent_id" label="Categoria pai (ULID)" value="{{ old('parent_id', $category->parent?->public_id ?? '') }}"/><x-input name="sort_order" type="number" label="Ordem" value="{{ old('sort_order', $category->sort_order ?? 0) }}"/><label class="flex items-center gap-2"><input type="checkbox" name="is_active" value="1" @checked(old('is_active', $category->is_active ?? true))> Ativa</label><label class="block text-sm font-medium" for="description">Descrição</label><textarea id="description" name="description" class="w-full rounded-md border-gray-300">{{ old('description', $category->description ?? '') }}</textarea></div><div class="mt-6 flex gap-3"><x-button>Salvar</x-button><a class="rounded-md px-4 py-2 text-sm" href="{{ route('catalog.categories.index') }}">Cancelar</a></div>
