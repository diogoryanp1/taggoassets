<x-layouts.app><x-breadcrumb>Catálogo / Categorias</x-breadcrumb><div class="mb-5 flex items-start justify-between gap-4"><div><h1 class="font-[Sora] text-2xl font-bold">Categorias de ativos</h1><p class="mt-1 text-sm text-gray-500">Classificação hierárquica do patrimônio.</p></div>@can('create', \App\Domain\Assets\Models\AssetCategory::class)<x-button onclick="location.href='{{ route('catalog.categories.create') }}'">Nova categoria</x-button>@endcan</div><x-filter-panel method="GET"><x-input name="search" value="{{ request('search') }}" placeholder="Buscar categoria"/><x-button>Aplicar filtros</x-button></x-filter-panel><x-table><thead><tr><th class="p-3">Categoria</th><th>Código</th><th>Status</th><th>Subcategorias</th><th></th></tr></thead><tbody>@forelse($categories as $category)<tr><td class="p-3 font-medium">{{ $category->name }}</td><td>{{ $category->code ?? '—' }}</td><td><x-badge>{{ $category->is_active ? 'Ativo' : 'Inativo' }}</x-badge></td><td>{{ $category->children_count }}</td><td>@can('update', $category)<a class="text-teal-700" href="{{ route('catalog.categories.edit', $category) }}">Editar</a>@endcan</td></tr>@empty<tr><td colspan="5" class="p-8 text-center text-gray-500">Nenhuma categoria cadastrada.</td></tr>@endforelse</tbody></x-table><x-pagination :paginator="$categories"/></x-layouts.app>
