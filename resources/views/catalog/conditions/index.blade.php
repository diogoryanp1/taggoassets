<x-layouts.app><x-breadcrumb>Catalogo / Condicoes</x-breadcrumb><div class="mb-5 flex items-start justify-between gap-4"><div><h1 class="font-[Sora] text-2xl font-bold">Estados de Conservacao</h1><p class="mt-1 text-sm text-gray-500">Condicoes globais e personalizadas usadas no cadastro de ativos.</p></div>@can('create', \App\Domain\Assets\Models\AssetCondition::class)<x-button onclick="location.href='{{ route('catalog.conditions.create') }}'">Cadastrar condicao</x-button>@endcan</div><x-filter-panel method="GET"><x-input name="search" value="{{ request('search') }}" placeholder="Buscar condicao"/><x-select name="origin"><option value="">Sistema e tenant</option><option value="system" @selected(request('origin') === 'system')>Sistema</option><option value="tenant" @selected(request('origin') === 'tenant')>Personalizadas</option></x-select><x-button>Filtrar</x-button></x-filter-panel><x-table><thead><tr><th class="p-3">Nome</th><th>Codigo</th><th>Ordem</th><th>Origem</th><th>Status</th><th></th></tr></thead><tbody>@forelse($conditions as $condition)<tr><td class="p-3 font-medium">{{ $condition->name }}</td><td>{{ $condition->code }}</td><td>{{ $condition->sort_order }}</td><td><x-badge>{{ $condition->is_system ? 'Sistema' : 'Tenant' }}</x-badge></td><td>{{ $condition->is_active ? 'Ativa' : 'Inativa' }}</td><td class="space-x-3"><a class="text-teal-700" href="{{ route('catalog.conditions.show', $condition) }}">Detalhes</a>@can('update', $condition)<a class="text-teal-700" href="{{ route('catalog.conditions.edit', $condition) }}">Editar</a>@endcan</td></tr>@empty<tr><td colspan="6" class="p-8 text-center text-gray-500">Nenhuma condicao encontrada.</td></tr>@endforelse</tbody></x-table><x-pagination :paginator="$conditions"/></x-layouts.app>
