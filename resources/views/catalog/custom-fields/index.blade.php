<x-layouts.app><x-breadcrumb>Catalogo / Campos Customizados</x-breadcrumb><div class="mb-5 flex items-start justify-between gap-4"><div><h1 class="font-[Sora] text-2xl font-bold">Campos Customizados</h1><p class="mt-1 text-sm text-gray-500">Campos adicionais por categoria de ativo.</p></div>@can('create', \App\Domain\Assets\Models\AssetCustomFieldDefinition::class)<x-button onclick="location.href='{{ route('catalog.custom-fields.create') }}'">Cadastrar campo</x-button>@endcan</div><x-filter-panel method="GET"><x-input name="search" value="{{ request('search') }}" placeholder="Buscar campo"/><x-select name="category"><option value="">Todas as categorias</option>@foreach($categories as $category)<option value="{{ $category->public_id }}" @selected(request('category') === $category->public_id)>{{ $category->name }}</option>@endforeach</x-select><x-button>Filtrar</x-button></x-filter-panel><x-table><thead><tr><th class="p-3">Nome</th><th>Chave</th><th>Categoria</th><th>Tipo</th><th>Status</th><th></th></tr></thead><tbody>@forelse($fields as $field)<tr><td class="p-3 font-medium">{{ $field->name }}</td><td>{{ $field->key }}</td><td>{{ $field->category->name }}</td><td>{{ $field->field_type }}</td><td><x-badge>{{ $field->is_active ? 'Ativo' : 'Inativo' }}</x-badge></td><td class="space-x-3"><a class="text-teal-700" href="{{ route('catalog.custom-fields.show', $field) }}">Detalhes</a>@can('update', $field)<a class="text-teal-700" href="{{ route('catalog.custom-fields.edit', $field) }}">Editar</a>@endcan</td></tr>@empty<tr><td colspan="6" class="p-8 text-center text-gray-500">Nenhum campo customizado encontrado.</td></tr>@endforelse</tbody></x-table><x-pagination :paginator="$fields"/></x-layouts.app>
