<x-layouts.app><x-breadcrumb>Catalogo / Campos / Detalhes</x-breadcrumb><div class="mb-5 flex items-start justify-between"><div><h1 class="font-[Sora] text-2xl font-bold">{{ $field->name }}</h1><p class="text-sm text-gray-500">{{ $field->category->name }} / {{ $field->key }}</p></div><x-badge>{{ $field->is_active ? 'Ativo' : 'Inativo' }}</x-badge></div><x-card><dl class="grid gap-4 text-sm sm:grid-cols-2"><div><dt class="text-text-secondary">Tipo</dt><dd class="font-medium">{{ $field->field_type }}</dd></div><div><dt class="text-text-secondary">Obrigatorio</dt><dd class="font-medium">{{ $field->is_required ? 'Sim' : 'Nao' }}</dd></div><div><dt class="text-text-secondary">Ordenacao</dt><dd class="font-medium">{{ $field->sort_order }}</dd></div><div><dt class="text-text-secondary">Opcoes</dt><dd class="font-medium">{{ collect($field->options ?? [])->join(', ') ?: '-' }}</dd></div></dl><div class="mt-6 flex gap-3">@can('update', $field)<x-button type="button" onclick="location.href='{{ route('catalog.custom-fields.edit', $field) }}'">Editar</x-button>@endcan @can('deactivate', $field)<form method="POST" action="{{ $field->is_active ? route('catalog.custom-fields.deactivate', $field) : route('catalog.custom-fields.reactivate', $field) }}">@csrf @method('PATCH')<x-button variant="secondary">{{ $field->is_active ? 'Inativar' : 'Reativar' }}</x-button></form>@endcan</div></x-card></x-layouts.app>
