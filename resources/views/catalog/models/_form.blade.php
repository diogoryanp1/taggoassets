<div class="grid gap-4 md:grid-cols-2"><x-select name="asset_brand_id" label="Marca" required><option value="">Selecione</option>@foreach($brands as $brand)<option value="{{ $brand->public_id }}" @selected(old('asset_brand_id', $model->brand?->public_id ?? '') === $brand->public_id)>{{ $brand->name }}</option>@endforeach</x-select><x-select name="asset_type_id" label="Tipo"><option value="">Todos os tipos</option>@foreach($types as $type)<option value="{{ $type->public_id }}" @selected(old('asset_type_id', $model->type?->public_id ?? '') === $type->public_id)>{{ $type->name }}</option>@endforeach</x-select><x-input name="name" label="Nome" value="{{ old('name', $model->name ?? '') }}" required/><x-input name="manufacturer_code" label="Codigo do fabricante" value="{{ old('manufacturer_code', $model->manufacturer_code ?? '') }}"/><label class="flex items-center gap-2 text-sm"><input type="checkbox" name="is_active" value="1" @checked(old('is_active', $model->is_active ?? true))> Ativo</label><div class="md:col-span-2"><label class="mb-1 block text-sm font-medium">Descricao</label><textarea name="description" class="w-full rounded-control border-border text-sm">{{ old('description', $model->description ?? '') }}</textarea></div></div><div class="mt-6 flex gap-3"><x-button>Salvar</x-button><a class="rounded-md px-4 py-2 text-sm" href="{{ route('catalog.models.index') }}">Cancelar</a></div>
