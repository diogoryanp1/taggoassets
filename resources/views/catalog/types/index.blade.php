<x-layouts.app><x-breadcrumb>Catalogo / Tipos</x-breadcrumb><div class="mb-5 flex items-start justify-between gap-4"><div><h1 class="font-[Sora] text-2xl font-bold">Tipos</h1><p class="mt-1 text-sm text-gray-500">Classificacao operacional dos ativos por categoria.</p></div>@can('create', \App\Domain\Assets\Models\AssetType::class)<x-button onclick="location.href='{{ route('catalog.types.create') }}'">Cadastrar tipo</x-button>@endcan</div><x-filter-panel method="GET"><x-input name="search" value="{{ request('search') }}" placeholder="Buscar tipo"/><x-select name="category"><option value="">Todas as categorias</option>@foreach($categories as $category)<option value="{{ $category->public_id }}" @selected(request('category') === $category->public_id)>{{ $category->name }}</option>@endforeach</x-select><x-select name="active"><option value="">Ativos e inativos</option><option value="1" @selected(request('active') === '1')>Ativos</option><option value="0" @selected(request('active') === '0')>Inativos</option></x-select><x-button>Filtrar</x-button></x-filter-panel><x-table><thead><tr><th class="p-3">Nome</th><th>Categoria</th><th>Codigo</th><th>Status</th><th></th></tr></thead><tbody>@forelse($types as $type)<tr><td class="p-3 font-medium">{{ $type->name }}</td><td>{{ $type->category->name }}</td><td>{{ $type->code ?? '-' }}</td><td><x-badge>{{ $type->is_active ? 'Ativo' : 'Inativo' }}</x-badge></td><td class="space-x-3"><a class="text-teal-700" href="{{ route('catalog.types.show', $type) }}">Detalhes</a>@can('update', $type)<a class="text-teal-700" href="{{ route('catalog.types.edit', $type) }}">Editar</a>@endcan</td></tr>@empty<tr><td colspan="5" class="p-8 text-center text-gray-500">Nenhum tipo encontrado.</td></tr>@endforelse</tbody></x-table><x-pagination :paginator="$types"/></x-layouts.app>
