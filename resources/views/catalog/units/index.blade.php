<x-layouts.app><x-breadcrumb>Catalogo / Unidades de Medida</x-breadcrumb><div class="mb-5 flex items-start justify-between gap-4"><div><h1 class="font-[Sora] text-2xl font-bold">Unidades de Medida</h1><p class="mt-1 text-sm text-gray-500">Unidades globais do sistema e unidades personalizadas do tenant.</p></div>@can('create', \App\Domain\Assets\Models\UnitOfMeasure::class)<x-button onclick="location.href='{{ route('catalog.units.create') }}'">Cadastrar unidade</x-button>@endcan</div><x-filter-panel method="GET"><x-input name="search" value="{{ request('search') }}" placeholder="Buscar unidade"/><x-select name="type"><option value="">Todos os tipos</option>@foreach(['unit','weight','length','area','volume','currency','time','other'] as $type)<option value="{{ $type }}" @selected(request('type') === $type)>{{ $type }}</option>@endforeach</x-select><x-select name="origin"><option value="">Sistema e tenant</option><option value="system" @selected(request('origin') === 'system')>Sistema</option><option value="tenant" @selected(request('origin') === 'tenant')>Personalizadas</option></x-select><x-button>Filtrar</x-button></x-filter-panel><x-table><thead><tr><th class="p-3">Nome</th><th>Simbolo</th><th>Tipo</th><th>Origem</th><th>Status</th><th></th></tr></thead><tbody>@forelse($units as $unit)<tr><td class="p-3 font-medium">{{ $unit->name }}</td><td>{{ $unit->symbol }}</td><td>{{ $unit->type }}</td><td><x-badge>{{ $unit->is_system ? 'Sistema' : 'Tenant' }}</x-badge></td><td>{{ $unit->is_active ? 'Ativa' : 'Inativa' }}</td><td class="space-x-3"><a class="text-teal-700" href="{{ route('catalog.units.show', $unit) }}">Detalhes</a>@can('update', $unit)<a class="text-teal-700" href="{{ route('catalog.units.edit', $unit) }}">Editar</a>@endcan</td></tr>@empty<tr><td colspan="6" class="p-8 text-center text-gray-500">Nenhuma unidade encontrada.</td></tr>@endforelse</tbody></x-table><x-pagination :paginator="$units"/></x-layouts.app>
