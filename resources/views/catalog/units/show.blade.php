<x-layouts.app><x-breadcrumb>Catalogo / Unidades / Detalhes</x-breadcrumb><div class="mb-5 flex items-start justify-between"><div><h1 class="font-[Sora] text-2xl font-bold">{{ $unit->name }}</h1><p class="text-sm text-gray-500">{{ $unit->symbol }} / {{ $unit->type }}</p></div><x-badge>{{ $unit->is_system ? 'Sistema' : 'Tenant' }}</x-badge></div><x-card><p class="mb-6 text-sm text-gray-600">Casas decimais: {{ $unit->decimal_places }}. Status: {{ $unit->is_active ? 'ativa' : 'inativa' }}.</p><div class="flex gap-3">@can('update', $unit)<x-button type="button" onclick="location.href='{{ route('catalog.units.edit', $unit) }}'">Editar</x-button>@endcan @can('deactivate', $unit)<form method="POST" action="{{ $unit->is_active ? route('catalog.units.deactivate', $unit) : route('catalog.units.reactivate', $unit) }}">@csrf @method('PATCH')<x-button variant="secondary">{{ $unit->is_active ? 'Inativar' : 'Reativar' }}</x-button></form>@endcan</div></x-card></x-layouts.app>
