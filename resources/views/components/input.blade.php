@props(['label' => null, 'name', 'help' => null]) @if($label)<label for="{{ $name }}" class="mb-1 block text-sm font-medium text-text-primary">{{ $label }}</label>@endif <input id="{{ $name }}" name="{{ $name }}" aria-describedby="{{ $help ? $name.'-help' : '' }}" {{ $attributes->merge(['class' => 'w-full rounded-control border-border text-sm shadow-sm focus:border-primary focus:ring-primary']) }} value="{{ old($name, $attributes->get('value')) }}">@if($help)<p id="{{ $name }}-help" class="mt-1 text-xs text-text-secondary">{{ $help }}</p>@endif @error($name)<p class="mt-1 text-xs text-danger" role="alert">{{ $message }}</p>@enderror
