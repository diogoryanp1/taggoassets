@props(['label' => null, 'name']) @if($label)<label for="{{ $name }}" class="mb-1 block text-sm font-medium text-text-primary">{{ $label }}</label>@endif <textarea id="{{ $name }}" name="{{ $name }}" {{ $attributes->merge(['class' => 'w-full rounded-control border-border text-sm shadow-sm focus:border-primary focus:ring-primary']) }}>{{ old($name, $slot) }}</textarea>@error($name)<p class="mt-1 text-xs text-danger" role="alert">{{ $message }}</p>@enderror
