<x-layouts.guest><x-card class="w-full max-w-md"><h1 class="font-[Sora] text-2xl font-bold">Ativar conta</h1><p class="mt-2 text-sm">{{ $invitation->email }}</p><form method="POST" action="{{ route('invitations.complete', $invitation) }}" class="mt-5 space-y-4">@csrf<input type="hidden" name="token" value="{{ request('token') }}"><x-input name="password" type="password" label="Defina sua senha" required/><x-input name="password_confirmation" type="password" label="Confirme a senha" required/><x-button class="w-full">Ativar conta</x-button></form></x-card></x-layouts.guest>
