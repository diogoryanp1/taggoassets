<x-layouts.app><x-breadcrumb>Convites / Novo</x-breadcrumb><x-card class="max-w-xl"><form method="POST" action="{{ route('invitations.store') }}" class="space-y-4">@csrf<x-input name="name" label="Nome" required/><x-input name="email" label="E-mail" type="email" required/><x-select name="role" label="Perfil">@foreach($roles as $role)<option value="{{ $role->name }}">{{ $role->label }}</option>@endforeach</x-select><x-button>Enviar convite</x-button></form></x-card></x-layouts.app>
