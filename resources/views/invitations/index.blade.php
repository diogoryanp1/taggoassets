<x-layouts.app><x-breadcrumb>Convites</x-breadcrumb><div class="mb-5 flex justify-between"><h1 class="font-[Sora] text-2xl font-bold">Convites</h1><x-button type="button" onclick="location.href='{{ route('invitations.create') }}'">Novo convite</x-button></div><x-table><thead><tr><th class="p-3">Nome</th><th>E-mail</th><th>Expira</th><th>Status</th><th></th></tr></thead><tbody>@foreach($invitations as $invitation)<tr><td class="p-3">{{ $invitation->name }}</td><td>{{ $invitation->email }}</td><td>{{ $invitation->expires_at }}</td><td>{{ $invitation->accepted_at ? 'Aceito' : ($invitation->revoked_at ? 'Revogado' : ($invitation->expires_at->isPast() ? 'Expirado' : 'Pendente')) }}</td><td>@if(! $invitation->accepted_at && ! $invitation->revoked_at)<form method="POST" action="{{ route('invitations.resend', $invitation) }}">@csrf<button class="text-teal-700">Reenviar</button></form>@endif</td></tr>@endforeach</tbody></x-table><x-pagination :paginator="$invitations"/></x-layouts.app>
