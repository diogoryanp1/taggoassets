<x-layouts.app><x-breadcrumb>Sessões</x-breadcrumb><h1 class="mb-5 font-[Sora] text-2xl font-bold">Sessões ativas</h1><x-table><thead class="bg-gray-50"><tr><th class="px-5 py-3">Dispositivo</th><th>IP</th><th>Última atividade</th><th></th></tr></thead><tbody class="divide-y">@foreach($sessions as $session) @php($device = app(\App\Support\UserAgent\UserAgentParser::class)->parse($session->user_agent))<tr><td class="px-5 py-4">{{ $device->displayName() }}</td><td>{{ preg_replace('/(\d+)$/', '***', $session->ip_address) }}</td><td>{{ $session->last_activity_at }}</td><td><form method="POST" action="{{ route('sessions.destroy', $session) }}">@csrf @method('DELETE')<button class="text-red-700">Encerrar</button></form></td></tr>@endforeach</tbody></x-table><form class="mt-5" method="POST" action="{{ route('sessions.destroy-others') }}">@csrf @method('DELETE')<x-input name="password" type="password" label="Confirme sua senha para encerrar outras sessões" required/><x-button class="mt-3">Encerrar outras sessões</x-button></form></x-layouts.app>
